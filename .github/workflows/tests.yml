name: .NET Tests

on:
  pull_request:
    types: [opened, reopened]
    branches: 
      - main
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: dotnet restore
        run: dotnet restore ./RealEstateAgency/RealEstateAgency.sln

      - name: Build solution
        run: dotnet build ./RealEstateAgency/RealEstateAgency.sln --no-restore

      - name: Run tests
        run: dotnet test ./RealEstateAgency/RealEstateAgency.Test/RealEstateAgency.Test.csproj --no-build --verbosity detailed